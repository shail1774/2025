wsl
https://www.youtube.com/watch?v=cJWhyycbPyA
https://www.youtube.com/watch?v=N1B_hAYzM1M&t=28s
https://www.youtube.com/watch?v=DmfuJzX6vJQ
wsl -l -v
wsl --list --online
wsl --install OracleLinux_9_5
wsl --shutdown
wsl --unregister debian
wsl --unregister ubuntu


k8s cluster
https://microk8s.io/#install-microk8s
https://kubernetes.io/docs/tasks/tools/install-kubectl-linux/#install-kubectl-binary-with-curl-on-linux
sudo install -o root -g root -m 0755 kubectl /usr/local/bin/kubectl
microk8s status --wait-ready
curl -LO "https://dl.k8s.io/release/$(curl -L -s https://dl.k8s.io/release/stable.txt)/bin/linux/amd64/kubectl"
sudo install -o root -g root -m 0755 kubectl /usr/local/bin/kubectl
kubectl version
cd $HOME
mkdir .kube
cd .kube
microk8s config > config
kubectl get nodes 
kubectl get all -A
done

helm - https://jhooq.com/getting-start-with-helm-chart/
curl -fsSL -o get_helm.sh https://raw.githubusercontent.com/helm/helm/master/scripts/get-helm-3
chmod 700 get_helm.sh
./get_helm.sh
helm version

helm

helm install myhelloworld helloworld
helm delete myhellworld
helm list -a


helm create helloworld
helm install test1 helloworld
helm list -a
hostname -I
microk8s dashboard-proxy
https://hostip:10443

helm list -a
helm uninstall test1


upgrade app
helm upgrade myhelloworldrelease helloworld

rollback app with revision no
helm list -a
helm rollback myhelloworldrelease 1


Validate helm chart before install
helm --debug --dry-run

helm install myhelloworld --debug --dry-run helloworld

Helm Template
renders the chart templates locally

helm template helloworld

Helm Lint - Find any misconfiguration or error

helm lint helloworld

helm uninstall - Remove the chart

helm uninstall myhelloworldrelease


github.com/rahulwagh/python-flask-rest-api-project

docker build
docker tag
docker push


create helm chart
helm create phython-flask-rest-api-project

>>>>>helmfile - https://jhooq.com/helmfile-manage-helmchart/

helmfile sync
helmfile -version

add plugin - helm-git
helm plugin install https://github.com/aslafy-z/helm-git --version 1.4.0

>>> Helm repo

helm search hub wordpress
helm search hub wordpress  --max-col-width=0
(complete url of helm chart repo)

helm repo list
helm repo add bitnami https://charts.bitnami.com/bitnami

helm repo list
helm show readme bitnami/wordpress --version 15.2.13


>> helm hooks & test

